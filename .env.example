# Telegram Bot Configuration
# Get your bot token from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# Telegram group ID to monitor (use negative number for groups, e.g., -1001234567890)
TELEGRAM_GROUP_ID=-1001234567890

# Notion API Configuration
# Create an integration at https://www.notion.so/my-integrations
NOTION_API_KEY=secret_your_notion_api_key_here

# Your Notion database ID (found in the database URL)
NOTION_DATABASE_ID=your_database_id_here

# Kimi (Moonshot AI) API Configuration
# Get your API key from https://platform.moonshot.cn/console/api-keys
KIMI_API_KEY=sk-your_kimi_api_key_here

# Kimi model selection (choose one):
#   moonshot-v1-8k   - 8K context, cheapest
#   moonshot-v1-32k  - 32K context
#   moonshot-v1-128k - 128K context, for long decks
#   kimi-k2.5        - Latest model
KIMI_MODEL=kimi-k2.5

# DocSend Configuration (for automated deck extraction)
DOCSEND_EMAIL=your-team-email@company.com
DOCSEND_PASSWORD=optional_password_if_required

# Optional: DocSend extraction mode
# "auto" = API first, Playwright fallback (default)
# "api" = API only, no browser (saves memory on low-RAM servers)
# DOCSEND_EXTRACTION_MODE=auto

# Optional: Message grouping timeout in seconds (default: 30)
MESSAGE_GROUPING_TIMEOUT=30

# Optional: OCR language for pdf2llm (default: chi_sim+eng)
OCR_LANGUAGE=chi_sim+eng

# Optional: Telegram proxy (required in China)
# TELEGRAM_PROXY=socks5://127.0.0.1:1080

# =============================================================================
# Cleanup Settings (Important for Cloud Deployment)
# =============================================================================

# Clean up PDFs immediately after text extraction (default: false)
# Set to "true" for aggressive cleanup in memory-constrained environments
CLEANUP_AFTER_EXTRACT=false

# Delete temp files older than this many minutes (default: 1440 = 24 hours)
CLEANUP_MAX_AGE_MINUTES=1440

# How often to run periodic cleanup in minutes (default: 1440 = 24 hours)
CLEANUP_INTERVAL_MINUTES=1440
